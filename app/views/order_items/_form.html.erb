<%= form_with(model: order_item, local: true) do |form| %>
  <% if order_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_item.errors.count, "error") %> prohibited this order_item from being saved:</h2>

      <ul>
      <% order_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id, id: :order_item_id %>
  </div>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id, id: :order_item_order_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :order_item_product_id %>
  </div>

  <div class="field">
    <%= form.label :qty_order %>
    <%= form.number_field :qty_order, id: :order_item_qty_order %>
  </div>

  <div class="field">
    <%= form.label :qty_ship %>
    <%= form.number_field :qty_ship, id: :order_item_qty_ship %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost, id: :order_item_cost %>
  </div>

  <div class="field">
    <%= form.label :retail %>
    <%= form.text_field :retail, id: :order_item_retail %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
