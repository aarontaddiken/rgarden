<p id="notice"><%= notice %></p>

<h1>Order Items</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Order</th>
      <th>Product</th>
      <th>Qty order</th>
      <th>Qty ship</th>
      <th>Cost</th>
      <th>Retail</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @order_items.each do |order_item| %>
      <tr>
        <td><%= order_item.id %></td>
        <td><%= order_item.order %></td>
        <td><%= order_item.product %></td>
        <td><%= order_item.qty_order %></td>
        <td><%= order_item.qty_ship %></td>
        <td><%= order_item.cost %></td>
        <td><%= order_item.retail %></td>
        <td><%= link_to 'Show', order_item %></td>
        <td><%= link_to 'Edit', edit_order_item_path(order_item) %></td>
        <td><%= link_to 'Destroy', order_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order Item', new_order_item_path %>
