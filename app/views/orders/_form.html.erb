<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ponum %>
    <%= form.text_field :ponum, id: :order_ponum %>
  </div>

  <div class="field">
    <%= form.label :order_date %>
    <%= form.date_select :order_date, id: :order_order_date %>
  </div>

  <div class="field">
    <%= form.label :request_ship_date %>
    <%= form.date_select :request_ship_date, id: :order_request_ship_date %>
  </div>

  <div class="field">
    <%= form.label :invoice_date %>
    <%= form.date_select :invoice_date, id: :order_invoice_date %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total, id: :order_total %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note, id: :order_note %>
  </div>

  <div class="field">
    <%= form.label :contact_id %>
    <%= form.text_field :contact_id, id: :order_contact_id %>
  </div>

  <div class="field">
    <%= form.label :seed_year %>
    <%= form.number_field :seed_year, id: :order_seed_year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
