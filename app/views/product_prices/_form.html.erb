<%= form_with(model: product_price, local: true) do |form| %>
  <% if product_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_price.errors.count, "error") %> prohibited this product_price from being saved:</h2>

      <ul>
      <% product_price.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :product_price_product_id %>
  </div>

  <div class="field">
    <%= form.label :billto_id %>
    <%= form.text_field :billto_id, id: :product_price_billto_id %>
  </div>

  <div class="field">
    <%= form.label :retail %>
    <%= form.text_field :retail, id: :product_price_retail %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost, id: :product_price_cost %>
  </div>

  <div class="field">
    <%= form.label :seed_year %>
    <%= form.number_field :seed_year, id: :product_price_seed_year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
